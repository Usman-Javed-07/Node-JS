//  modules

// CommonJS, every file is module (by defalut)
// Modules - Encapsulated Code (only share minimum)



const names = require('./4-names') 

console.log(names)
;


// sayhii('usman');
// sayhii(ali);
// sayhii(saad);